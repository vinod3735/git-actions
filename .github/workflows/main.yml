name: github_actions_file
on: 
  push:
    branches:
      - main
      - feature/cam
      
  

jobs:
  git_hub_actions:
    runs-on: ubuntu-latest
    steps:
      - name: echo hello world
      - run: echo " Hello world"
